package {{extra.package}}

public interface {{className}} {
{{#methods}}
  {{method}}()
  {{#if responses}}{{#each responses}}{{#if this.body}}
    {{@key}}
     {{#each this.body}}{{#if this.schema}}
      {{#parseSchema this.schema}}
      {{title}}
      {{/parseSchema}}
     {{/if}}{{/each}}
  {{/if}}{{/each}}{{/if}}
{{/methods}}
}
